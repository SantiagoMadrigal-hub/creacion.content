"""Capa de dominio: entidades, puertos y servicios de negocio puros.

Este paquete no depende de ninguna infraestructura concreta (APIs HTTP,
SDKs de terceros, FFmpeg, sistema de archivos). Solo conoce abstracciones
(``Protocol``) definidas en :mod:`lavox.domain.ports`.
"""
